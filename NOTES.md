# Notes

- Workspace deliberately kept separate from `me&dev/shuff/` (user's choice) — this is a general WebExtensions fluency track, not a "build shuff" track, even though shuff is used as the running example.
- shuff's own purpose/features are not yet defined by the user (as of 2026-08-30) — don't assume what it does beyond "Firefox extension, currently just a manifest.json + empty popup." Ask again later if it becomes relevant, or infer once more of shuff-browser exists.
- shuff-browser stack observed: Manifest V3, `web-ext` for dev running (`web-ext-config.cjs` points at a local Firefox binary in `.firefox-dev/`).
